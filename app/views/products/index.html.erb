<ul>
  <% cart.each do |item|%>
    <li><%= item %></li>
  <% end %>
</ul>

<h3>Add Item:</h3><br />
<%= form_for :product, url: add_product_path do |f| %>
  product:
  <%= f.text_field :product, name: "product" %> <br />
  <%= f.submit 'add to cart'%>
<% end %>
